# Copy to .env and adjust as needed.
#
# Base URL for the xShot AI backend API.
#   empty                -> use the Vite dev proxy (vite.config.js) -> localhost:8000
#   https://<host>        -> call that origin directly
VITE_API_URL=https://xshotai.up.railway.app
