# xShotAI
xShot AI is a basketball training analytics app: upload practice video from a fixed camera; the pipeline detects shot attempts, classifies makes and misses, and shows FG%. Built with React (Vite), Python FastAPI, and a YOLOv8 rolling-window CV model for ball and hoop. No manual labeling.
